const Obj = {
    js : 'Javascript',
    cpp :'C++',
    rb : 'ruby',
    swift : 'swift by apple'
}
for (const key in Obj) {
    // console.log(key);  // print only key
    // console.log(Obj[key]);  // print only value 
    // console.log(`${key} shortcut is for ${Obj[key]}`); //print both key and value
}

const arr = ["js", "jb", "rb","c++"];
for (const key in arr) {
//    console.log(key);  // output me index value aaegi like - 0, 1, 2 etc.
//    console.log(arr[key]); // here print value of arr
}
